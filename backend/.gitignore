node_modules
dist

# Keep environment variables out of version control.
# Covers .env, .env.development, .env.production — everything except the
# committed template.
.env
.env.*
!.env.example

# Prisma generated client
/src/generated

# Local storage driver output (dev uploads)
/uploads

# Migration snapshots + reports (contain live store data)
/migration-backups
